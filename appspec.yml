version: 0.0
os: linux

files:
    -   source: /
        destination: /home/ubuntu/todo
        overwrite: yes
permissions:
    -   object: /
        owner: ubuntu
        group: ubuntu

hooks:
    AfterInstall:
        -   location: scripts/stop.sh
            timeout: 60
    ApplicationSTart:
        -   location: scripts/start.sh
            timeout: 60
